import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('ConfigurationPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ConfigurationPage(title: 'Configuration')));
    //  final titleFinder = find.text('Configuration');
    //  expect(titleFinder, findsOneWidget);
  });
}
