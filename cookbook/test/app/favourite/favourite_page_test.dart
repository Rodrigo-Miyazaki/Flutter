import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('FavouritePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FavouritePage(title: 'Favourite')));
    //  final titleFinder = find.text('Favourite');
    //  expect(titleFinder, findsOneWidget);
  });
}
