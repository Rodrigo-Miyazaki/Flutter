import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('SecurityPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SecurityPage(title: 'Security')));
    //  final titleFinder = find.text('Security');
    //  expect(titleFinder, findsOneWidget);
  });
}
